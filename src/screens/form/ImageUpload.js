import { View} from 'react-native';
import React from 'react';

// an example of component in Add page
const ImageUpload = ({ navigation }) => {
    return (
        <View >
        </View>
    );
}

export default ImageUpload;
