import Icon from "react-native-vector-icons/AntDesign";
import React from "react";

const IconAtomQR = ({ name, size, style }) => (
  <Icon name={name} size={size} style={style} />
);

export default IconAtomQR;
