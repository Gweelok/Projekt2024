import { StyleSheet } from "react-native";
import indexStyles from "../../styles/index";

const onHideViewStyles = StyleSheet.create({
  width: indexStyles.metrics.width.none,
  height: indexStyles.metrics.height.none,
});

export default onHideViewStyles;
